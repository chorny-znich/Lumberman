/*#pragma once

#include <string>
#include <vector>

#include "Lumberman.h"

class LevelManager
{
private:
public:
	void init();
	void createLevels(std::string id);
	std::vector<dr::StaticObject> getStaticObjects(std::string id);
	std::vector<dr::DynamicObject> getDynamicObject(std::string id);
};

*/